sub kmain(initrd)
	local fd

	gdt_init()
	page_init()
	tss_init()
	idt_init()
	heap_init()
	global_init()
	kunpack(initrd)
	idt_sti()
	kcls()
	kputs("Booting...")
	kputch(10)
	sys_exec("/bin/pish.fap", 0, 0)
	user_jmp()
end
