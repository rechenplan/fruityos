--- /mnt/data/phobos_under8/sol/mars/assemble.hr	2026-07-20 04:17:22.695861777 +0000
+++ /mnt/data/phobos_attempt7/sol/mars/assemble.hr	2026-07-20 04:45:52.462158502 +0000
@@ -50,9 +50,33 @@
   while padding > 0 do mars_byte(state, 0) padding = padding - 1 end
 end
 
+
+sub mars_dead_runtime_name(name, length)
+  if mars_slice_equal(name, length, "__haruka_alloc", 14) then return 1 end
+  if mars_slice_equal(name, length, "__haruka_lift", 13) then return 1 end
+  return 0
+end
+
+sub mars_dead_runtime_symbol(state:MarsState, name, length)
+  local symbol:MarsSymbol
+  if mars_dead_runtime_name(name, length) == 0 then return 0 end
+  symbol = mars_find_symbol(state, name, length)
+  if symbol == 0 then return 1 end
+  return symbol.referenced == 0
+end
+
 sub mars_layout_item(state:MarsState, item:SolItem)
   local size
   if item.kind != 9 then state.zero_valid = 0 end
+  if state.skip_dead_runtime then
+    if (item.kind == 5) || (item.kind == 6) || (item.kind == 10) then state.skip_dead_runtime = 0
+    else return 0 end
+  end
+  if ((item.kind == 5) || (item.kind == 6)) && mars_dead_runtime_symbol(state, item.name, item.name_length) then return 0 end
+  if (item.kind == 4) && mars_dead_runtime_symbol(state, item.name, item.name_length) then
+    state.skip_dead_runtime = 1
+    return 0
+  end
   if item.kind == 2 then return 0 end
 
   if item.kind == 3 then
@@ -108,6 +132,15 @@
 sub mars_emit_item(state:MarsState, item:SolItem)
   local size, next_ip, zero_valid, symbol:MarsSymbol
   if item.kind != 9 then state.zero_valid = 0 end
+  if state.skip_dead_runtime then
+    if (item.kind == 5) || (item.kind == 6) || (item.kind == 10) then state.skip_dead_runtime = 0
+    else return 0 end
+  end
+  if ((item.kind == 5) || (item.kind == 6)) && mars_dead_runtime_symbol(state, item.name, item.name_length) then return 0 end
+  if (item.kind == 4) && mars_dead_runtime_symbol(state, item.name, item.name_length) then
+    state.skip_dead_runtime = 1
+    return 0
+  end
   if item.kind == 2 then return 0 end
 
   if item.kind == 3 then
@@ -162,6 +195,7 @@
     state.origin_set = state.base_origin_set
     state.address = state.base_origin
     state.zero_valid = 0
+    state.skip_dead_runtime = 0
     if mars_micro_enabled(state) then mars_emit_micro_kernel(state) end
     done = 0
     while done == 0 do
@@ -180,6 +214,7 @@
   sol_reset(state.parser)
   state.address = state.origin
   state.zero_valid = 0
+  state.skip_dead_runtime = 0
   if mars_micro_enabled(state) then mars_emit_micro_kernel(state) end
   while 1 do
     item = mars_next_item(state)
--- /mnt/data/phobos_under8/sol/mars/main.hr	2026-07-19 15:26:17.000000000 +0000
+++ /mnt/data/phobos_attempt7/sol/mars/main.hr	2026-07-20 04:45:52.461869853 +0000
@@ -2,7 +2,7 @@
   local input_count, index = 0, path, output_path, map_path, map_only = 0
   local remaining = argc - 1, parsing_options = 1
   local head:SolSource, tail:SolSource, source:SolSource
-  local parser:SolParser, state:MarsState = sol_alloc(112), output:MarsOutput = sol_alloc(24)
+  local parser:SolParser, state:MarsState = sol_alloc(120), output:MarsOutput = sol_alloc(24)
 
   path = mars_next_argument(argv)
   while (remaining > 0) && parsing_options do
--- /mnt/data/phobos_under8/sol/mars/model.hr	2026-07-19 15:26:17.000000000 +0000
+++ /mnt/data/phobos_attempt7/sol/mars/model.hr	2026-07-20 04:45:52.461619396 +0000
@@ -2,5 +2,5 @@
 record MarsOutput fd, buffer, used
 record MarsState parser:SolParser, symbols:MarsSymbol,
        output:MarsOutput, origin, origin_set, base_origin, base_origin_set,
-       address, allow_unresolved,
+       address, allow_unresolved, skip_dead_runtime,
        measuring, measure_count, image_size, round, zero_valid
--- /mnt/data/phobos_under8/sol/phobos/assemble.hr	2026-07-20 04:17:22.695393559 +0000
+++ /mnt/data/phobos_attempt7/sol/phobos/assemble.hr	2026-07-20 04:45:52.462948144 +0000
@@ -50,9 +50,33 @@
   while padding > 0 do mars_byte(state, 0) padding = padding - 1 end
 end
 
+
+sub mars_dead_runtime_name(name, length)
+  if mars_slice_equal(name, length, "__haruka_alloc", 14) then return 1 end
+  if mars_slice_equal(name, length, "__haruka_lift", 13) then return 1 end
+  return 0
+end
+
+sub mars_dead_runtime_symbol(state:MarsState, name, length)
+  local symbol:MarsSymbol
+  if mars_dead_runtime_name(name, length) == 0 then return 0 end
+  symbol = mars_find_symbol(state, name, length)
+  if symbol == 0 then return 1 end
+  return symbol.referenced == 0
+end
+
 sub mars_layout_item(state:MarsState, item:SolItem)
   local size
   if item.kind != 9 then state.zero_valid = 0 end
+  if state.skip_dead_runtime then
+    if (item.kind == 5) || (item.kind == 6) || (item.kind == 10) then state.skip_dead_runtime = 0
+    else return 0 end
+  end
+  if ((item.kind == 5) || (item.kind == 6)) && mars_dead_runtime_symbol(state, item.name, item.name_length) then return 0 end
+  if (item.kind == 4) && mars_dead_runtime_symbol(state, item.name, item.name_length) then
+    state.skip_dead_runtime = 1
+    return 0
+  end
   if item.kind == 2 then return 0 end
 
   if item.kind == 3 then
@@ -108,6 +132,15 @@
 sub mars_emit_item(state:MarsState, item:SolItem)
   local size, next_ip, zero_valid, symbol:MarsSymbol
   if item.kind != 9 then state.zero_valid = 0 end
+  if state.skip_dead_runtime then
+    if (item.kind == 5) || (item.kind == 6) || (item.kind == 10) then state.skip_dead_runtime = 0
+    else return 0 end
+  end
+  if ((item.kind == 5) || (item.kind == 6)) && mars_dead_runtime_symbol(state, item.name, item.name_length) then return 0 end
+  if (item.kind == 4) && mars_dead_runtime_symbol(state, item.name, item.name_length) then
+    state.skip_dead_runtime = 1
+    return 0
+  end
   if item.kind == 2 then return 0 end
 
   if item.kind == 3 then
@@ -162,6 +195,7 @@
     state.origin_set = state.base_origin_set
     state.address = state.base_origin
     state.zero_valid = 0
+    state.skip_dead_runtime = 0
     if mars_micro_enabled(state) then mars_emit_micro_kernel(state) end
     done = 0
     while done == 0 do
@@ -180,6 +214,7 @@
   sol_reset(state.parser)
   state.address = state.origin
   state.zero_valid = 0
+  state.skip_dead_runtime = 0
   if mars_micro_enabled(state) then mars_emit_micro_kernel(state) end
   while 1 do
     item = mars_next_item(state)
--- /mnt/data/phobos_under8/sol/phobos/main.hr	2026-07-19 15:26:17.000000000 +0000
+++ /mnt/data/phobos_attempt7/sol/phobos/main.hr	2026-07-20 04:45:52.462709921 +0000
@@ -2,7 +2,7 @@
   local input_count, index = 0, path, output_path, map_path, map_only = 0
   local remaining = argc - 1, parsing_options = 1
   local head:SolSource, tail:SolSource, source:SolSource
-  local parser:SolParser, state:MarsState = sol_alloc(112), output:MarsOutput = sol_alloc(24)
+  local parser:SolParser, state:MarsState = sol_alloc(120), output:MarsOutput = sol_alloc(24)
 
   path = mars_next_argument(argv)
   while (remaining > 0) && parsing_options do
--- /mnt/data/phobos_under8/sol/phobos/model.hr	2026-07-19 15:26:17.000000000 +0000
+++ /mnt/data/phobos_attempt7/sol/phobos/model.hr	2026-07-20 04:45:52.462468563 +0000
@@ -2,5 +2,5 @@
 record MarsOutput fd, buffer, used
 record MarsState parser:SolParser, symbols:MarsSymbol,
        output:MarsOutput, origin, origin_set, base_origin, base_origin_set,
-       address, allow_unresolved,
+       address, allow_unresolved, skip_dead_runtime,
        measuring, measure_count, image_size, round, zero_valid
