# Haruka / Phobos full OS verification

This directory records the clean full OS build and QEMU/OVMF boot verification.

The exact tested files are also present at:

- `out/fruityos.efi`
- `out/fruityos_uefi.img`

Expected boot log:

```
U12345EHBooting...
Welcome to FruityOS 0.8.1!
/>
```
